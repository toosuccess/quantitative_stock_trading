# 交易执行页面计算逻辑修复计划

## 问题分析

### 问题 1：剩余股数计算
**位置 1**：TradeExecution.vue 执行步骤对话框中的"剩余数量"字段（第 195-197 行）
**位置 2**：执行步骤表格中（可能需要添加列）
**当前状态**：✅ 对话框中的计算已正确（getRemainingQuantity 函数：计划数量 - 持仓数量）
**需要确认**：执行步骤表格中是否需要显示"持仓数量"和"剩余数量"列

### 问题 2：持仓金额计算
**位置**：执行步骤对话框或表格中
**当前状态**：❌ 未显示持仓金额字段
**需要添加**：持仓金额 = 持仓股数 × 当前股价

## 实施步骤

### 步骤 1：确认需求 ✅
根据用户选中的 td 元素，确认需要：
1. 在执行步骤表格中添加"持仓数量"和"剩余数量"列
2. 在执行步骤表格中添加"持仓金额"列，或确保对话框中计算正确

### 步骤 2：执行步骤表格添加列
在 `el-table` (filteredSteps) 中添加：
- **持仓数量列**：显示 `scope.row.holding_quantity`
- **剩余数量列**：显示 `scope.row.planned_quantity - scope.row.holding_quantity`
- **持仓金额列**（可选）：显示 `scope.row.holding_quantity * scope.row.current_price`

### 步骤 3：验证计算逻辑
- 剩余数量：`getRemainingQuantity()` 函数已正确实现
- 持仓金额：需要在对话框中添加计算和显示

### 步骤 4：验证测试
- 前端服务热重载验证
- 打开执行步骤对话框检查数值
- 验证计算公式是否正确

## 预期效果
1. 执行步骤表格显示：持仓数量、剩余数量（= 计划数量 - 持仓数量）
2. 对话框中显示正确的剩余数量
3. 持仓金额计算正确（= 持仓股数 × 当前股价）

## 相关文件
- `d:\workspace\quantitative_stock_trading\frontend\src\views\TradeExecution.vue`

## 修改方案

### 方案 A：在执行步骤表格中添加列
在步骤表格（第 46-91 行）中添加：
```vue
<el-table-column prop="holding_quantity" label="持仓数量" width="100" />
<el-table-column label="剩余数量" width="100">
  <template #default="scope">
    {{ scope.row.planned_quantity - (scope.row.holding_quantity || 0) }}
  </template>
</el-table-column>
<el-table-column label="持仓金额" width="120">
  <template #default="scope">
    {{ ((scope.row.holding_quantity || 0) * (scope.row.current_price || 0)).toFixed(2) }}
  </template>
</el-table-column>
```

### 方案 B：仅在对话框中修复（如果表格不需要）
对话框中的计算已正确，无需修改。
